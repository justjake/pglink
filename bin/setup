#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."
eval "$(mise activate bash --shims)"

# Install mise tools
mise install

# Configure git to use .githooks directory
git config core.hooksPath .githooks

echo "Setup complete. Git hooks configured."
